---
exo__Asset_uid: 2821fdf0-bf65-4afe-96cd-59da980ffe84
exo__Asset_isDefinedBy: "[[f8630146-3f10-4dc2-9b49-4beb6884956e]]"
exo__Asset_createdAt: 2026-09-09T11:26:11+05:00
exo__Asset_updatedAt: 2026-09-25T09:20:00
exo__Asset_createdBy: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
exo__Instance_class:
  - "[[8c5af681-3413-4219-8636-0ac229d1b253|req__Requirement]]"
exo__Asset_label: "req(ems): «Create Action» on a Task nests the action under that task — ems__Effort_parent = the task; Effort_parent range widened to ems__Effort"
req__Requirement_status: "[[fccf8fa4-8004-41ee-9102-595a588e9be7|req__RequirementStatusActive]]"
req__Requirement_approvedBy: "[[0aa339bc-9b56-400a-8148-cbde57bbf0b6|a.kitelev]]"
req__Requirement_approvedAt: 2026-09-09T11:26:11+05:00
req__Requirement_priority: "[[481c3be1-d05c-4b78-8a3c-c61308d40bf1|req__RequirementPriorityP2]]"
req__Requirement_bindingClass:
  - "[[f8841786-64c2-42a9-8b45-2d33fd6be87c|req__RequirementBindingClassIntegration]]"
req__Requirement_area: "[[bd76637d-5788-4c30-a3d8-c88dcdd9970f|Exocortex Development]]"
req__Requirement_author: "[[de20a3f1-7483-4714-ab28-b45f5cf02c76|ExoAssistant]]"
req__Requirement_covers:
  - "ems effort decomposition — an ems__Action created from an ems__Task target nests under that task. (1) PARENT ON TASK TARGETS: when the homoiconic command «Create Action» (48a70029, grounding 1bc1e938) is applied to an ems__Task, the created ems__Action carries ems__Effort_parent = that task. Before this requirement the action was created with no ems__Effort_parent at all — the grounding declared only an Area rule (3f08f5a8), a Project-conditioned parent rule (43731bae) and an isDefinedBy pass-through (cbe000c4), and an ems__Task matches none of them; the action silently lost its home, which is the most common flow of the executive-assistant loop. (2) NO REGRESSION ON PROJECT TARGETS: the same command on an ems__Project still sets ems__Effort_parent to that project. (3) PROTOTYPES STAY PARENT-LESS: a prototype target (ems__TaskPrototype and siblings) still receives no ems__Effort_parent — their relation to the created instance is the exo__Asset_prototype back-link, and the command's precondition 365095bc refuses prototype targets outright. This is the regression guard for the 2026-05-24 defect that narrowed 43731bae from an exclusion form to a Project condition. (4) NON-EFFORT TARGETS: a target that is neither an Effort subclass nor an Area receives no ems__Effort_parent, so no value outside the property's range is ever written (the precondition admits status-less targets, e.g. a concept__Concept, so this is reachable via the CLI). (5) ONTOLOGY: exo__Property_range of ems__Effort_parent (6528ecfa) is ems__Effort (086f71fa), widened from ems__ParentEffort (17c5cf45) — ems__Task is not an ems__ParentEffort, so the write would otherwise be a sh:class violation. Since ems__ParentEffort is itself a subclass of ems__Effort the widening only relaxes the shape: every previously valid value stays valid and no data is migrated. ems__ParentEffort is NOT deprecated; it remains the Project/Initiative marker class. Named consequence, accepted by the founder: the SHACL gate «an internal WBS node MUST be an ems__Project» (decision 2026-06-20) stops being mechanically enforced for every class and survives as a convention only. (6) MECHANISM: the Task-conditioned InheritanceRule (65acce2f, uid -> ems__Effort_parent, condition ems__Task, priority 50) is attached to the Universal Default Template (62907ff4) next to the existing Project rule (01f570c9), and grounding 1bc1e938 declares no parent rule at all. This placement is forced by the engine, not chosen for style: mergeInheritanceRules deduplicates by targetPropertyName, so a grounding can express at most one rule per property and that rule also shadows every universal rule for the same property — declaring both a Project and a Task parent rule on the grounding loses whichever is not last (measured in both orders on a vault copy). The template's own list is not deduplicated against itself, so the two condition-based rules coexist there. Zero engine code — the change is vault data in exoas-public and exoas-exocmd. (7) ISOLATION: the five other effort-creating groundings (a222094b «Create task», 8748f8b0 «Create project», 4919afb1 «Create related task via service», b7a84c3c, d53c8726) each declare 43731bae themselves, which overrides both universal parent rules, so their behaviour is unchanged — no Create-Task-to-Task subtask binding is introduced. CORRECTION 2026-09-25 (task 0fe20951, measured on vault-exodev with --use-cache; canary: 21 groundings carry at least one exocmd__Grounding_inheritanceRule): the isolation set is SIX, not five — 4d8d5055 «Create task for daily note (declarative)» also declares 43731bae and is missing from the list above, so it was never part of the isolation measurement. It was created 2026-04-08, long before this requirement was written (2026-09-09), so the undercount happened in the original measurement and is not staleness of the list. Its behaviour is unchanged for the same mechanical reason, and that is now shown rather than inferred: executing mergeInheritanceRules from packages/core/dist on the measured inputs (universal = the 8 Template_inheritanceRule entries of 62907ff4, grounding = the 3 rules that all six share — 3f08f5a8, 43731bae, cbe000c4) leaves BOTH 01f570c9 and 65acce2f absent from the output, while the control run with 43731bae removed keeps both. The set is now expressed as the query that produces it, not as a frozen list, so it cannot drift again: SELECT ?g WHERE { ?g exocmd:Grounding_inheritanceRule ?r . FILTER(CONTAINS(STR(?r),'43731bae')) }."
req__Requirement_activatedAt: 2026-09-09T12:03:43+05:00
req__Requirement_implementedBy:
  - "Vault data only, zero engine code. (1) kitelev/exoas-public@443549a706b2 — exo__Property_range of ems__Effort_parent (6528ecfa) widened ems__ParentEffort -> ems__Effort (086f71fa); bodies of 6528ecfa and 17c5cf45 record the decision and that ParentEffort stays a class but is no longer a range. (2) kitelev/exoas-exocmd@e31de817e556 — new exocmd__InheritanceRule 65acce2f (exo__Asset_uid -> ems__Effort_parent, condition ems__Task, priority 50) attached to the Universal Default Template 62907ff4 beside the Project rule 01f570c9; BOTH parent rules removed from grounding 1bc1e938 (\"Create action\"), because mergeInheritanceRules deduplicates by targetPropertyName and a grounding therefore expresses at most one rule per property while also shadowing the universal ones — measured in both orders on a vault copy. Bodies of 1bc1e938, 48a70029 and 62907ff4 corrected (they claimed \"Project/Effort target -> parent\", cited the wrong precondition, and listed 6 of 8 template rules). 43731bae itself untouched and still declared by the five other effort-creating groundings. (3) kitelev/exoas-ci@09c4bf8aac6a — opt-in command_smoke step in the reusable assetspace-ci workflow; enabled for exoas-exocmd in f808d581f907. (4) kitelev/exocortex#4221 (merge 0b514aa2ac301e1d17f63cbbf27a894b107cb80e) — the two @req tests, the packages/exoas-exocmd submodule bump and the re-synced reference copy of the CI workflow."
req__Requirement_verifiedBy:
  - "packages/core/tests/integration/dynamic-commands/create-action-parent.integration.test.ts — production-shape markdown fixtures through the real NoteToRDFConverter.convertVault -> CommandResolver.loadGroundingByUid -> GroundingExecutor.execute pipeline, reading the created file back; four target shapes (Task -> parent = the task; Project -> unchanged; TaskPrototype -> no parent; concept__Concept -> no parent). REVERT-VERIFY: seeding without the Task rule reddens EXACTLY the Task axis (1 failed / 7 passed) while Project, prototype and non-Effort stay green. The fixture injects a class-label->UID resolver because the CLI and plugin both pass one — without it every parent axis is vacuously \"no parent\" (measured: the first version of the test passed its prototype axes while never exercising the rule). packages/core/tests/integration/dynamic-commands/create-action-parent-live.test.ts — data-guard over the real packages/exoas-exocmd checkout: both parent rules on the template, the Task rule's shape, and NO ems__Effort_parent rule on the grounding. REVERT-VERIFY: with the submodule pinned back to 829e06b all three axes go red; on the shipped pointer all three pass. CI: exoas-ci command_smoke applies create-action to a seeded ems__Task in the merged vault and requires ems__Effort_parent on the created asset — green on exoas-exocmd; measured red when the rule is unwired, and red on its own canary when the fixture's TBox is absent. Live CLI on a copy of vault-my: create-action on a Task -> parent = the task; on a Project -> parent = the project; on a TaskPrototype -> refused by precondition 365095bc; on a concept__Concept -> created without a parent. SHACL merged-vault: 0 violations, 625 warnings (baseline-identical). Isolation of the five sibling groundings measured: create-task and create-project produce byte-identical results before and after."
---

# req(ems): «Create Action» on a Task nests the action under that task

## Job Story

When I am looking at a task and realise it needs a concrete next physical step,
I want the «Create Action» button on that task to produce an action that already
belongs to it — so the action shows up under the task in every roll-up, and I
never have to wire the parent by hand or, worse, discover weeks later that a
pile of actions is floating with no home.

## Statement (Gherkin)

```gherkin
Scenario: Task target gets the task as parent
  Given an ems__Task with uid <task-uid> in a non-terminal status
  When «Create Action» is applied to that task
  Then the created ems__Action has ems__Effort_parent referencing <task-uid>

Scenario: Project target is unchanged (no regression)
  Given an ems__Project with uid <project-uid>
  When «Create Action» is applied to that project
  Then the created ems__Action has ems__Effort_parent referencing <project-uid>

Scenario: Prototype target still gets no parent
  Given an ems__TaskPrototype
  When «Create Action» is resolved for that prototype
  Then the command is not offered, and no ems__Action whose ems__Effort_parent
       points at the prototype can be produced through it

Scenario: Non-Effort target gets no parent
  Given a target whose class is neither an ems__Effort subclass nor an ems__Area
  When «Create Action» is applied to it
  Then the created ems__Action has no ems__Effort_parent property at all
  And therefore no value outside the range of ems__Effort_parent is written

Scenario: The ontology admits a Task as a parent
  Given the ems TBox after this requirement
  Then exo__Property_range of ems__Effort_parent is ems__Effort
  And SHACL-lite validation of the merged vault reports 0 violations

Scenario: Sibling create-commands are unaffected
  Given the commands «Create task» and «Create project»
  When either is applied to an ems__Task or an ems__Project target
  Then the resulting asset's ems__Effort_parent is exactly what it was before
       this requirement
```

## Non-goals

- No Create-Task→Task binding, and no change to `create-related-task` /
  `create-next-iteration` / `create-action-instance` — scope is «Create Action»
  only (founder decision #3).
- `ems__ParentEffort` is not deprecated.
- The 72 prototype-created parent-less Actions and the one live parent-less
  non-prototype Action are not migrated.
- The engine is not changed. In particular `classRefMatchesAny` keeps matching
  conditions directly and does **not** walk `exo__Class_superClass` — walking it
  would re-introduce the 2026-05-24 prototype defect.

## Provenance

Founder interview 2026-09-09, recorded as 8 `dec__Fork` assets in
`exoas-decisions` with subject `0e914ac3-64bd-4e13-896a-d64b3a3e8d30`
(the vault task carrying the full plan). GitHub umbrella issue
`kitelev/exocortex#4220`.

Approved on the strength of those forks: decision #1 (widen the range),
decision #2 (a Task-conditioned inheritance rule), decision #3 (scope = Create
Action only), decision #4 (integration test on fixtures + a contract test over
the live submodule), decision #5 (opt-in CI smoke).

⚠ Decision #2's *mechanism* was adjusted during implementation — the rule is
attached to the Universal Default Template rather than to the grounding —
because the literal placement is impossible under `mergeInheritanceRules`
(measured, see `covers` §6). The observable end-state the founder approved is
unchanged.

## ⛤ Уточнение 2026-09-25 — половина механизма из §(6) БОЛЬШЕ НЕ ДЕЙСТВУЕТ (req a2c868e9, PR #4348)

⛔ Исходная формулировка §(6) НЕ стёрта намеренно: она — принятое решение и объясняет, почему Task-правило `65acce2f` было размещено на шаблоне, а не на grounding. Читать её следует как верную НА МОМЕНТ принятия.

Что изменилось в движке (`mergeInheritanceRules`, `UniversalDefaultTemplateResolver.ts`):

| утверждение §(6) | статус после a2c868e9 |
|---|---|
| «a grounding can express at most one rule per property» | ⛔ **больше не верно** — grounding может объявить несколько условных правил на одно свойство, все сохраняются |
| «that rule also shadows every universal rule for the same property» | ✅ **остаётся верным по построению** — override сознательно оставлен ключуемым по ИМЕНИ свойства |
| «The template's own list is not deduplicated against itself» | ✅ верно, и дополнительно: повторные universal-правила на одно свойство больше не заставляют эмитить grounding-override дважды |

⚠ Вторая строка — не побочный эффект, а ЦЕЛЬ: дедуп по паре `(targetPropertyName, targetClassCondition)` расшторил бы `65acce2f` шести groundings, объявляющим `43731bae`, и ввёл бы ровно тот Create-Task-to-Task parent binding, который §(7) объявляет НЕ вводимым. Поэтому размещение Task-правила на шаблоне, выбранное в §(6), **остаётся рабочим и после снятия ограничения** — переносить его на grounding не требуется.

⛤ Отдельно заведён тикет `0fe20951-094a-4cf5-82e7-7cbab972c640`: §(7) перечисляет ПЯТЬ groundings с правилом `43731bae`, живых по замеру 2026-09-25 — ШЕСТЬ (не назван `4d8d5055` «Create task for daily note (declarative)»). Сам §(7) здесь не правлен: пересмотр объёма изоляционной проверки — предмет его владельца, а не этого уточнения.

⛔ Та же коррекция относится к ПОСЛЕДНЕЙ строке §Provenance: «the literal placement is impossible under `mergeInheritanceRules`». После `a2c868e9` буквальное размещение ОБОИХ правил на grounding стало возможным — движок их больше не теряет. Но оно по-прежнему **неверно** по другой причине, и причина эта важнее прежней: правило, объявленное grounding'ом, затеняет ВСЕ universal-правила того же свойства, поэтому перенос Task-правила на grounding `1bc1e938` отключил бы для этой команды оба template-правила. ⇒ решение #2 остаётся в силе, изменилось только его обоснование: не «невозможно», а «затенение делает это регрессией».

⛤ Кто внёс: сессия developer-d8, тикет `c3de23a8`, PR kitelev/exocortex#4348. Исходные формулировки сохранены намеренно — они фиксируют состояние механизма на момент принятия решения 2026-09-09.
